name: session_handoff
description: End session with compaction and memory bank updates.
steps:
  - session(operation="compact", summary="...")
  - "[memory bank updates if not done]"
  - update_memory_bank(operation="progress_append"|"active_context_append", ...) as needed
keywords:
  - handoff
  - end session
  - compact
  - session
  - wrap up

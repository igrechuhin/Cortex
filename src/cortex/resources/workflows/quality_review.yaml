name: quality_review
description: Review code quality and apply fixes.
steps:
  - session(operation="start")
  - load_context(task_description="review quality", token_budget=10000)
  - run_quality_gate()
  - run_quality_gate()
  - suggest_refactoring (if needed)
  - "[execute fixes]"
keywords:
  - quality
  - review
  - lint
  - format
  - refactor
  - pre-commit

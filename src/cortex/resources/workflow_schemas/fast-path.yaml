name: fast-path
description: plan → implement → commit (no review gate)
phases:
  - name: plan
    tool: /cortex/plan
    required: true
  - name: implement
    tool: /cortex/do
    required: true
  - name: commit
    tool: /cortex/commit
    required: true

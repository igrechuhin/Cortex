# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
ENV/
env/
.env

# Node (legacy local markdown tooling; no longer required)
node_modules/

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Logs
*.log
logs/

# Local development settings
.env.local
.env.development.local
.env.test.local
.env.production.local

# Credentials and secrets (do not commit)
*.pem
*.key
credentials.json
secrets.json
*credentials*.json
*secrets*.json

# Coverage reports
htmlcov/
.tox/
.coverage
.coverage.*
.cache
coverage.xml
coverage.json
coverage_consolidated.json
*.cover

# Cortex data directories (project-specific, not tracked in this repo)
# These are tracked in user projects, not in the Cortex server itself
/.cursor/

# Legacy memory bank paths (for migration)
/.memory-bank/
/.memory-bank-*
/.memory-bank-history/
/.memory-bank-summaries/
/memory-bank/
/.plan/

# Cortex backup directories
.cortex-backup-*
.session

# Test/benchmark artifacts (must not be created at repo root; use .cortex or tmp)
/benchmark_results/
.cortex/benchmark_results/

# Eval A/B comparison artifact (from run_eval_check --output-summary)
eval_results.json

# Sibling/legacy project copies (use only .cortex paths in repo root)
/schema/
/optimization/
/structure/
/consolidation/
/invalid/
/learning/
/validation/

# Cortex MCP (transient/generated files)
.cortex/.session/
.cortex/.cache/
.cortex/history/
.cortex-backup-*/

# Cortex MCP (transient/generated files)
.cortex/.session/
.cortex/.cache/
.cortex/history/
.cortex/.session/session-goal.md
.cortex-backup-*/

# Cortex agent sync outputs (generated at MCP startup — do not commit)
.claude/
.cursor/agents/
/.cortex/wal/write_log.jsonl

# Cortex MCP (transient/generated files)
.cortex/.session/
.cortex/.cache/
.cortex/history/
.cortex-backup-*/

# Cortex agent sync outputs (generated at MCP startup — do not commit)
.claude/
.cursor/agents/

# Cortex local machine context (host-specific; do not commit)
.cortex/memory-bank/local-environment-context.json

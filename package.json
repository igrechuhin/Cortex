{
  "name": "cortex-mcp",
  "version": "0.0.0",
  "private": true,
  "description": "Legacy Node metadata; markdown lint now uses rumdl via the Python environment.",
  "devDependencies": {},
  "engines": {
    "node": ">=18"
  }
}
